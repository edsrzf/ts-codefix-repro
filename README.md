This repo is an attempt at a minimal reproduction case for https://github.com/microsoft/TypeScript/issues/41962.

To make it happen, install dependencies with `npm install` and then run `npm run reproduce`.
